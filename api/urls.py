from api import views
from django.urls import path

urlpatterns = [
    # path("users/", views.UsersView.as_view(), name="users_view"),
    # path("user/<id_arg>", views.UserView.as_view(), name="user_view"),
    # path("user/", views.UserView.as_view(), name="user_create_view"),
    # path("user/update/<id_arg>", views.UserView.as_view(), name="user_view"),
    # path("user/delete/<id_arg>", views.UserView.as_view(), name="user_view"),

]